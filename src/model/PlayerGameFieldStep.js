export default class PlayerGameFieldStep {
  constructor() {
    this.closed = true;
    this.flag = false;
  }
}

/**
 * constructor(playerGameState) {
    this.playerGameState = playerGameState;
    this.closed = this.playerGameState.closed;
    this.flag = this.playerGameState.flag;
  }
 */
/**

 */
